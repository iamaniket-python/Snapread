{
 
  "provider": "python",
  "buildAptPackages": [],
  "steps": {
    "install": {
      "commands": [
        "cd Snapread && pip install -r requirements.txt"
      ]
    }
  },
  "deploy": {
    "startCommand": "cd Snapread && python manage.py migrate && gunicorn snapread.wsgi:application --bind 0.0.0.0:$PORT"
  }
}